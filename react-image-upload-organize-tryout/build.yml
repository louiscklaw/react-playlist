react-image-upload-organize-tryout:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - run: |
        pushd react-image-upload-organize-tryout
          yarn
          yarn build
        popd