react-progressive-image-tryout:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - run: |
        pushd react-progressive-image-tryout
          yarn
          yarn build
        popd