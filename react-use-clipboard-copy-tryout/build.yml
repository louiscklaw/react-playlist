react-use-clipboard-copy-tryout:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - run: |
        pushd react-use-clipboard-copy-tryout
          yarn
          yarn build
        popd
